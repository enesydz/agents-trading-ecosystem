[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "routing"
version = "0.1.0"
description = "Multi-venue routing and arbitrage primitives"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/routing"]
