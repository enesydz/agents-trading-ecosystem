[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "risk"
version = "0.1.0"
description = "Risk management and validation engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "domain-models",
    "shared-core",
]

[tool.hatch.build.targets.wheel]
packages = ["src/risk"]

[tool.ruff]
line-length = 100

[tool.pyright]
include = ["src"]
