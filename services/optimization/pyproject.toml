[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "optimization"
version = "0.1.0"
description = "Online strategy optimization primitives"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/optimization"]
